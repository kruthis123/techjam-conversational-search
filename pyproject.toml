[project]
name = "techjam-conversational-search"
version = "0.1.0"
description = "Offline conversational product-search agent for the TechJam hackathon"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=2.0,<3.0",
    "sentence-transformers>=6.0,<7.0",
]

[tool.uv]
package = false
